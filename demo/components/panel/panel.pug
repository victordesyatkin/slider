mixin panel(options={})
    -
        let { classNames = [], sections = [] } = options;
        classNames = classNames.join(' '); 
    .panel(
        class=`${classNames}`
    )
        if (title)
            .panel__title.title
        .panel__sections
            each section, key in sections
                +section(section)

